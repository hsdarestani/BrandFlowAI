## Verification

- [ ] API tests pass
- [ ] Alembic has a single head
- [ ] TypeScript passes
- [ ] Next.js production build passes
- [ ] Production Compose validates and builds
- [ ] `/` redirects to `/fa`
- [ ] `/healthz` and `/api/health` pass after deployment
- [ ] Worker and scheduler containers are running
