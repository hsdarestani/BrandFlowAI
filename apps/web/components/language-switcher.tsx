'use client';
import Link from 'next/link';import {usePathname,useSearchParams} from 'next/navigation';import {locales,switchLocalePath,type Locale} from '@/lib/i18n';
export function LanguageSwitcher({locale}:{locale:string}){const path=usePathname();const search=useSearchParams();const suffix=search.toString()?`?${search.toString()}`:'';return <div className="flex rounded-full border border-white/10 bg-white/5 p-1">{locales.map(l=><Link key={l} href={switchLocalePath(path,l as Locale)+suffix} onClick={()=>{try{localStorage.setItem('smarbiz_locale',l);document.cookie=`smarbiz_locale=${l};path=/;max-age=31536000`}catch{}}} className={`rounded-full px-3 py-1 text-xs font-bold ${locale===l?'bg-cyan-400 text-slate-950':'text-slate-700 hover:bg-slate-50'}`}>{l.toUpperCase()}</Link>)}</div>}
