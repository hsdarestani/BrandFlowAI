'use client';
import Link from 'next/link';import {useParams,useRouter} from 'next/navigation';import {LanguageSwitcher} from '@/components/language-switcher';import {t} from '@/lib/i18n';
export default function Signup(){const {locale}=useParams<{locale:string}>();const d=t(locale);const r=useRouter();return <main className="grid min-h-screen place-items-center p-5"><div className="absolute end-5 top-5"><LanguageSwitcher locale={locale}/></div><form className="command-card w-full max-w-xl" onSubmit={e=>{e.preventDefault();localStorage.setItem('brandflow_onboarding','started');r.push(`/${locale}/onboarding`)}}><Link href={`/${locale}`} className="flex items-center gap-3"><span className="logo-mark">B</span><b>{d.common.brand}</b></Link><h1 className="mt-8 text-4xl font-black">{d.auth.signupTitle}</h1><label className="mt-6 block text-sm font-bold">{d.auth.name}</label><input className="field mt-2"/><label className="mt-4 block text-sm font-bold">{d.auth.email}</label><input className="field mt-2"/><label className="mt-4 block text-sm font-bold">{d.auth.password}</label><input className="field mt-2" type="password"/><button className="btn mt-6 w-full">{d.auth.submitSignup}</button></form></main>}
