'use client';import {use,useState} from 'react';import {AppShell} from '@/components/app-shell';import {t} from '@/lib/i18n';import Link from 'next/link';
export default function Page({params}:{params:Promise<{locale:string}>}){const {locale}=use(params);return <AppShell locale={locale}><Help locale={locale}/></AppShell>}
function Help({locale}:{locale:string}){const d=t(locale);const [topic,setTopic]=useState(d.help.topics[0]);return <div className="grid gap-6 lg:grid-cols-[18rem_1fr_18rem]"><aside className="panel p-4"><h1 className="mb-4 text-2xl font-black">{d.help.title}</h1>{d.help.topics.map((x:string)=><button className={`block w-full rounded-xl p-3 text-start ${x===topic?'bg-cyan-300/10':'hover:bg-white/[.04]'}`} onClick={()=>setTopic(x)} key={x}>{x}</button>)}</aside><article className="panel p-8"><p className="badge">{topic}</p><h2 className="mt-4 text-4xl font-black">{d.help.article}</h2><p className="muted mt-4">{d.help.subtitle}</p><ol className="mt-6 list-decimal space-y-3 ps-6">{d.help.steps.map((s:string)=><li key={s}>{s}</li>)}</ol></article><aside className="learning-card p-5"><h3 className="font-black">{d.help.related}</h3><div className="mt-4 flex flex-col gap-2"><Link className="chip" href={`/${locale}/onboarding`}>{d.actions.continueSetup}</Link><Link className="chip" href={`/${locale}/app/integrations`}>{d.actions.connectChannel}</Link></div></aside></div>}
