'use client';
import {useParams} from 'next/navigation';import {useState} from 'react';import {LanguageSwitcher} from '@/components/language-switcher';
export default function Approval(){const {locale,token}=useParams<{locale:string;token:string}>();const [status,setStatus]=useState('pending');return <main className="min-h-screen grid place-items-center p-4"><div className="absolute end-5 top-5"><LanguageSwitcher locale={locale}/></div><section className="card w-full max-w-lg"><span className="badge">Secure public approval</span><h1 className="mt-4 text-3xl font-black">Review content draft</h1><p className="muted mt-2">Mobile-first approval link. Token: {token}</p><div className="mt-5 rounded-[2rem] border border-white/10 bg-slate-950 p-4"><div className="h-52 rounded-3xl bg-gradient-to-br from-violet-500/30 to-cyan-400/20 p-4"><span className="badge">Instagram + Telegram</span></div><h2 className="mt-4 text-xl font-black">Summer clinic trust carousel</h2><p className="muted mt-2">Evidence-led caption, CTA and compliance score. Client can approve, reject or request an AI revision without login.</p></div><textarea className="field mt-4 min-h-28" placeholder="Tell AI what to change"/><label className="mt-3 flex gap-2"><input type="checkbox" defaultChecked/> Save learning to Brand Memory</label><div className="mt-4 grid grid-cols-2 gap-3"><button className="btn" onClick={()=>setStatus('approved')}>Approve</button><button className="chip" onClick={()=>setStatus('changes requested')}>Request edit</button><button className="chip" onClick={()=>setStatus('rejected')}>Reject</button><button className="chip" onClick={()=>setStatus('rescheduled')}>Reschedule</button></div><p className="mt-4 rounded-2xl bg-white/5 p-3">Current status: <b>{status}</b></p></section></main>}
